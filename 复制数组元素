int[] a = {10, 20, 30, 40, 50, 60};
int[] b = new int[6];
//a：源数组
//1：源数组的起始位置
//b：目标数组
//0：目标数组的起始位置
//4：要复制元素的个数
System.arraycopy(a, 1, b, 0, 4);
for (int i = 0; i < b.length; i++) {
    System.out.println(b[i]);
}
