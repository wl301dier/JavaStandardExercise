//声明数组a,b并分别进行初始化
int[] a = {10, 20, 30, 40, 50, 60};
int[] b = new int[6];
//a：源数组
//1：源数组的起始位置
//b：目标数组
//0：目标数组的起始位置
//4：要复制元素的个数
//复制数组元素，从数组a的第二个位置到末尾，选取4个元素，复制（覆盖掉）到从数组b的第一个位置到末尾的4个元素
System.arraycopy(a, 1, b, 0, 4);
//循环遍历数组b，并输出数组b的数据
for (int i = 0; i < b.length; i++) {
    System.out.println(b[i]);
}
